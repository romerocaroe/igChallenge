mutation updateLikeStatus($id: ID!, $description: String!) {
    updateDescription(id: $id, description: $description) {
        id
        createdDate
        updateDate
        imagePosted
        description
        amountOfLikes
        isLiked
    }
}